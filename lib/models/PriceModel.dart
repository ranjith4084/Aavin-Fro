class PriceModel{
  double total_gst;
  double total_sgst;
  double total_cgst;

  PriceModel(this.total_gst, this.total_sgst, this.total_cgst, this.total_igst,
      this.total_basicprice,this.total_price);

  double total_igst;
  double total_basicprice;
  double total_price;


}